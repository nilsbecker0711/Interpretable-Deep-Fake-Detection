# log dir (ignored — train_config.yaml wins via config.update(config2) in train.py:282)
log_dir: ./logs/convnext

# model setting
pretrained: Null

model_name: convnext_bcos   # model name
backbone_name: convnext_bcos  # backbone name

eval: true

#backbone setting
backbone_config:
  num_classes: 2
  in_chans: 6                  # NOTE: this backbone reads `in_chans`, not `channels`
  # variant presets defined in networks/base/convnext_bcos.py:113-146.
  # Available: atto (3.36M) | tiny (27.78M) | small (49.36M) | base (87.44M) | large (196.05M).
  # tiny is the closest preset to the ~21M cluster (vit_bcos 20.50M,
  # xception_bcos 20.79M, resnet34_bcos_v2 21.29M) — +32%, but the next step
  # down (atto) is 6x too small, so tiny it is.
  block_setting: tiny
  mode: original               # required key, but never read by the backbone (dead)
  stochastic_depth_prob: 0.0
  layer_scale: 1.0e-6
  logit_bias: #None
  logit_temperature: # None == 1, matches the original.
  b: 1.25
  # BatchNormUncentered2d matches the resnet34_bcos_v2 / xception_bcos recipe, so
  # architecture comparisons are not confounded by the norm scheme. Upstream calls
  # this variant convnext_*_bnu; DetachablePositionNorm2d is their _pn variant.
  norm: BatchNormUncentered2d
  norm_bias: false

# dataset
all_dataset: [FaceForensics++, FF-F2F, FF-DF, FF-FS, FF-NT]
train_dataset: [FaceForensics++]
test_dataset: [FaceForensics++]
val_dataset: [FaceForensics++]

compression: c23  # compression-level for videos
train_batchSize: 32   # training batch size
val_batchSize: 32   # val batch size
test_batchSize: 32   # test batch size
workers: 8   # number of data loading workers
frame_num: {'train': 32, 'test': 32, 'val': 32}   # number of frames to use per video in training and testing
resolution: 256   # resolution of output image to network
with_mask: false   # whether to include mask information in the input
with_landmark: false   # whether to include facial landmark information in the input
add_inverse_channels: true


# data augmentation
dataset_type: 'bcos'
use_data_augmentation: True  # Add this flag to enable/disable data augmentation
data_aug:
  flip_prob: 0.5
  rotate_prob: 0.5
  rotate_limit: [-10, 10]
  blur_prob: 0.5
  blur_limit: [3, 7]
  brightness_prob: 0.5
  brightness_limit: [-0.1, 0.1]
  contrast_limit: [-0.1, 0.1]
  quality_lower: 40
  quality_upper: 100

# mean and std for normalization
mean: [0.5, 0.5, 0.5]
std: [0.5, 0.5, 0.5]

# optimizer config
optimizer:
  # choose between 'adam' and 'sgd'
  type: adam
  # unified recipe (2026-07, b-cos paper) — identical across all models:
  adam:
    lr: 0.00025  # learning rate -> bcos paper 2.5e-4
    beta1: 0.8  # beta1 for Adam optimizer
    beta2: 0.999 # beta2 for Adam optimizer
    eps: 0.000001  # epsilon for Adam optimizer
    weight_decay: 0  # b-cos paper: unnecessary, weights normalized to unit norm
    amsgrad: true
  sgd:
    lr: 0.0002  # learning rate
    momentum: 0.9  # momentum for SGD optimizer
    weight_decay: 0.0005  # weight decay for regularization
  gradient_clipping: false

# training config
lr_scheduler: warmup_cosine # unified across all models (2026-07)
lr_eta_min: 0
lr_T_max: 100
nEpochs: 100   # number of epochs to train for
start_epoch: 0   # manual epoch number (useful for restarts)
save_epoch: 1   # interval epochs for saving models
rec_iter: 100   # interval iterations for recording
logdir: ./logs   # folder to output images and logs
manualSeed: 1024   # manual seed for random number generation
save_ckpt: true   # whether to save checkpoint
save_feat: true   # whether to save features

# in-training XAI monitor (GPG on fixed shared val grids + MPG on val fakes)
xai_monitor_every_n_epochs: 10   # 0 disables
xai_monitor_grid_dir: results/GPG_assets/shared_random/FaceForensics++_val_256/3x3
xai_monitor_mpg_images: 250

# loss function
loss_func: bce_off_labels   # loss function to use
losstype: null

# metric
metric_scoring: auc   # metric for evaluation (auc, acc, eer, ap)

# cuda
ddp: true   # overwritten in train.py -> main() -> config['ddp'] = args.ddp
cuda: true   # whether to use CUDA acceleration
cudnn: true   # whether to use CuDNN for convolution operations
